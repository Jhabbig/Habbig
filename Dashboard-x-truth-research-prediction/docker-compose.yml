services:
  dashboard:
    build: .
    ports:
      - "127.0.0.1:18789:18789"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/predictions.db
    restart: unless-stopped
