# Pinned for reproducibility + CVE safety. Review monthly; run pip-audit before bumping.
# Last audited: 2026-04-21 (AUDIT #3 CVE sweep — bumped 8 packages).
fastapi==0.115.0
uvicorn[standard]==0.29.0
httpx==0.27.0
python-multipart==0.0.26
websockets==12.0
cryptography==44.0.1
pyotp==2.9.0
qrcode[pil]==7.4.2
sentry-sdk[fastapi]==1.45.1
anthropic==0.30.0
logtail-python==0.2.0
# Bumped below: transitive deps pinned for supply-chain reproducibility +
# CVE closure (starlette multipart memory, Pillow image parsing, requests
# cert handling, python-dotenv parser, filelock race).
starlette==0.47.2
pillow==12.2.0
python-dotenv==1.2.2
requests==2.33.0
filelock==3.20.3
