# Pinned for reproducibility + CVE safety. Review monthly; run pip-audit before bumping.
# Last audited: 2026-04-21 (AUDIT #3 CVE sweep — bumped 8 packages).
fastapi==0.120.4  # bumped alongside starlette 0.49.1 for CVE compat (audit #2 — needs <0.50)
# orjson: used as FastAPI's default_response_class when present. ~3-5×
# faster than stdlib json on the list payloads returned by /api/feed,
# /api/markets, /api/sources. Optional — server.py falls back to stdlib
# JSONResponse if the import fails, so dev setups without the wheel
# still work.
orjson==3.11.6  # CVE-2025-67221 (audit #2 — pip-audit sweep)
uvicorn[standard]==0.29.0
httpx==0.27.0
python-multipart==0.0.26
websockets==12.0
cryptography==46.0.7  # CVE-2026-26007 + CVE-2026-34073 + CVE-2026-39892 (audit #2)
pyotp==2.9.0
qrcode[pil]==7.4.2
sentry-sdk[fastapi]==1.45.1
anthropic==0.30.0
logtail-python==0.2.0
apscheduler==3.10.4
# Bumped below: transitive deps pinned for supply-chain reproducibility +
# CVE closure (starlette multipart memory, Pillow image parsing, requests
# cert handling, python-dotenv parser, filelock race).
starlette==0.49.1  # CVE-2025-62727 (audit #2 — pip-audit sweep)
pillow==12.2.0
python-dotenv==1.2.2
requests==2.33.0
filelock==3.20.3
# Web Push (VAPID-signed notifications). Required for /api/push/* and the
# settings push toggle to work in production. push.py degrades to 503 if
# pywebpush is missing, so dev installs that skip these still boot.
pywebpush==2.1.2
py-vapid==1.9.4
http-ece==1.2.1
