# Pinned for reproducibility (audit #6 MEDIUM #3). Mirrors gateway/requirements.txt
# versions where overlapping; cross-referenced against repo-root requirements.lock.
fastapi==0.120.4
uvicorn[standard]==0.29.0
httpx==0.27.0
pydantic==2.13.4
pyyaml==6.0.3
sentry-sdk[fastapi]==1.45.1
logtail-python==0.2.0
